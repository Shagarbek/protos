syntax = "proto3";

package my_service;

option go_package = "github.com/Shagarbek/gen-repo/my_service";

service MyService {
  rpc Ping (Empty) returns (Pong);
}

message Empty {}
message Pong {
  string message = 1;
}